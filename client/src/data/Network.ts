/**
 * todo realease me
 * @param login string with login
 */
export function isUniqueLogin(login: string): boolean {
    return login != 'unique';
}

/**
 * todo realease me
 * @param email string with email address
 */
export function isUniqueEmail(email: string): boolean {
    return email != 'unique';
}
