import {actions as user} from './user';
import {actions as persons} from './persons';
import {actions as tab} from './tab';

const actions = {
    user,
    persons,
    tab
}

export default actions;
